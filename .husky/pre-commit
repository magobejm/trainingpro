#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "TrainerPro: Ejecutando verificaciones Pre-Commit..."

# 1. Lint & format (solo archivos modificados)
pnpm lint-staged

# 2. Typecheck (todo el workspace)
pnpm -r typecheck
