/// <reference types="expo/types" />

// NOTE: This file should not be edited and should be in your git ignore… but
// here we keep it tracked because the workspace tsconfig uses an explicit
// `include`/`types` set and we need the Expo ambient types (incl. `process.env`).
